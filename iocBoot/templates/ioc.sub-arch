# IOC archive file

file topas-ioc.archive
{
	{ IOC = "$$IF(IOCPVROOT)$$IOCPVROOT$$ELSE(IOCPVROOT)$$TRANSLATE(IOCNAME,"a-z_-","A-Z::")$$ENDIF(IOCPVROOT)" }
}

file topas.tpl-arch
{
	pattern
	{ BASE }
$$LOOP(TOPAS)
	{ "$$BASE" }
$$ENDLOOP(TOPAS)
}
