# IOC archive file

file topas-ioc.archive
{
	{ IOC = "$$IOC_PV" }
}

$$LOOP(TOPAS)
file topas.tpl-arch
{
	{ BASE = "$$BASE" }
}
$$ENDLOOP(TOPAS)

$$LOOP(CARBIDE)
file carbide.tpl-arch
{
	{ BASE = "$$BASE" }
}

file carbide_reg.arch
{
	{ BASE = "$$BASE" }
}
$$ENDLOOP(CARBIDE)
