# topas IOC instance autosave file

file topas-ioc.req
{
	{ IOC = "$$IOC_PV" }
}

file topas.tpl-req
{
	pattern
	{ BASE }
$$LOOP(TOPAS)
	{ "$$BASE" }
$$ENDLOOP(TOPAS)
}

file topas_motor.tpl-req
{
	pattern
	{ BASE, ID }
$$LOOP(MOTOR)
	{ "$$BASE", "$$ID" }
$$ENDLOOP(MOTOR)
}
