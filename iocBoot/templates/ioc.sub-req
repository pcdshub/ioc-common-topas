# topas IOC instance autosave file

file topas-ioc.req
{
	{ IOC = "$$IF(IOCPVROOT)$$IOCPVROOT$$ELSE(IOCPVROOT)$$TRANSLATE(IOCNAME,"a-z_-","A-Z::")$$ENDIF(IOCPVROOT)" }
}

file topas.tpl-req
{
	pattern
	{ BASE }
$$LOOP(TOPAS)
	{ "$$BASE" }
$$ENDLOOP(TOPAS)
}
